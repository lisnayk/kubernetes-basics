# Pods

> **Под** (англ. *Pod*) — це найменша одиниця, яку можна розгортати та керувати в Kubernetes. :contentReference[oaicite:1]{index=1}

Под — це група з одного або більше контейнерів, які мають спільні сховища, мережеві ресурси та специфікацію для запуску контейнерів. Вміст Пода завжди знаходиться в одному контексті та виконується спільно. :contentReference[oaicite:2]{index=2}

---

## 📌 Що таке Pod

- Под представляє **групу контейнерів**, що запускаються разом на одному вузлі. :contentReference[oaicite:3]{index=3}
- Контейнери в межах одного Пода **розділяють IP-адресу, портовий простір та томи (volumes)**. :contentReference[oaicite:4]{index=4}
- Під являє собою **логічний хост для контейнерів**, що тісно пов’язані між собою. :contentReference[oaicite:5]{index=5}

---

## 🧱 Основні компоненти Pod

### 🐳 Контейнери

- В Pod можуть входити **один або кілька контейнерів**.
- Контейнери всередині одного Пода працюють **разом** і можуть легко обмінюватися даними.

### 🌐 Мережа

- Кожен Pod отримує **унікальну IP-адресу** в межах кластера, що дозволяє контейнерам спілкуватися один з одним без конфліктів портів. :contentReference[oaicite:6]{index=6}

### 📁 Сховище (Volumes)

- Pod може включати томи, які дозволяють зберігати дані незалежно від життєвого циклу контейнера.

---

## 🔄 Життєвий цикл Pod

Поды проходять через кілька станів:

1. **Pending** — Pod створено, але ресурси ще не виділені. :contentReference[oaicite:7]{index=7}
2. **Running** — один або кілька контейнерів Pod запущено. :contentReference[oaicite:8]{index=8}
3. **Succeeded** — всі контейнери завершили роботу успішно. :contentReference[oaicite:9]{index=9}
4. **Failed** — принаймні один контейнер завершився з помилкою. :contentReference[oaicite:10]{index=10}

---

## 🤝 Як взаємодіють сервіси та Pod

У Kubernetes **сервіси** дозволяють логічно об'єднати декілька Pod, щоб забезпечити стабільний доступ до них через один IP або DNS-ім’я, незалежно від того, на яких вузлах ці Pod запущено. :contentReference[oaicite:11]{index=11}

---

## 🧠 Навіщо потрібні Pod

- Pods дозволяють запускати один або кілька тісно пов’язаних контейнерів на одному вузлі з розділеними ресурсами. :contentReference[oaicite:12]{index=12}
- Вони є **базовою одиницею розгортання** в Kubernetes — на їх основі створюються інші об’єкти (наприклад, ReplicaSet, Deployment і т.д.). :contentReference[oaicite:13]{index=13}

---

## 📦 Важливі властивості Pod

- Унікальна IP-адреса в межах кластера. :contentReference[oaicite:14]{index=14}
- Спільні томи для контейнерів у межах Pod. :contentReference[oaicite:15]{index=15}
- Контейнери запускаються і зупиняються разом. :contentReference[oaicite:16]{index=16}

---

## 🧾 Приклад (YAML)

> Ось типовий приклад опису Pod у форматі YAML:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: example-pod
spec:
  containers:
    - name: app-container
      image: nginx:latest
      ports:
        - containerPort: 80
```
## Static Pods

**Static Pods (статичні поди)** керуються безпосередньо демоном kubelet на конкретному вузлі, без спостереження з боку API-сервера.  
Тоді як більшість Pod керуються control plane (наприклад, через Deployment), для статичних Pod kubelet безпосередньо контролює кожен Pod (і перезапускає його у разі збою).

Статичні Pod завжди прив’язані до одного kubelet на конкретному вузлі.

Основне призначення статичних Pod — запуск самостійно розгорнутого (self-hosted) control plane. Іншими словами, kubelet використовується для контролю окремих компонентів control plane.

Kubelet автоматично намагається створити **mirror Pod** (дзеркальний Pod) в Kubernetes API-сервері для кожного статичного Pod.  
Це означає, що Pod, які працюють на вузлі, відображаються в API-сервері, але керувати ними звідти неможливо.

Детальніше дивіться в керівництві **Create static Pods**.

## Pods with multiple containers

Pod спроєктовані для підтримки кількох взаємодіючих процесів (у вигляді контейнерів), які утворюють цілісну сервісну одиницю.

Контейнери в межах одного Pod автоматично розміщуються (co-located) та плануються (co-scheduled) на одній фізичній або віртуальній машині в кластері.  
Контейнери можуть спільно використовувати ресурси та залежності, взаємодіяти між собою і координувати момент та спосіб завершення роботи.

---

## Основні способи використання Pod у Kubernetes

У кластері Kubernetes Pod використовуються двома основними способами:

### 1️⃣ Pod з одним контейнером

Модель «один контейнер — один Pod» є найпоширенішим сценарієм використання Kubernetes.

У цьому випадку Pod можна розглядати як обгортку навколо одного контейнера; Kubernetes керує саме Pod, а не контейнерами безпосередньо.

---

### 2️⃣ Pod з кількома контейнерами

Pod може інкапсулювати застосунок, що складається з кількох співрозміщених контейнерів, які тісно пов’язані між собою та мають спільні ресурси.

Такі контейнери формують єдину сервісну одиницю.

Наприклад:
- один контейнер публічно надає дані, що зберігаються у спільному томі (volume),
- окремий sidecar-контейнер оновлює або перезаписує ці файли.

Pod об’єднує ці контейнери, ресурси зберігання та тимчасову мережеву ідентичність в одну логічну одиницю.

Інший приклад:
- один контейнер працює як вебсервер для файлів у спільному томі,
- окремий sidecar-контейнер оновлює ці файли з віддаленого джерела.

---

## Init-контейнери

Деякі Pod містять init-контейнери разом із контейнерами застосунку (app containers).

За замовчуванням init-контейнери:
- запускаються першими,
- повинні повністю завершитися,
- і лише після цього запускаються основні контейнери застосунку.

---

## Sidecar-контейнери

Pod також можуть містити sidecar-контейнери, які надають допоміжні сервіси основному застосунку (наприклад, компоненти service mesh).

---

## Стан функціональності

**FEATURE STATE: Kubernetes v1.33 [stable] (увімкнено за замовчуванням)**

Функціональність `SidecarContainers`, увімкнена за замовчуванням, дозволяє вказувати `restartPolicy: Always` для init-контейнерів.

Встановлення політики перезапуску `Always` гарантує, що контейнери, для яких вона задана, будуть розглядатися як sidecar-контейнери та залишатимуться запущеними протягом усього життєвого циклу Pod.

Контейнери, які явно визначені як sidecar-контейнери:
- запускаються перед основним контейнером застосунку,
- працюють протягом усього часу існування Pod,
- завершуються лише під час зупинки Pod.

Приклади
```shell
kubectl create namespace lec2
kubectl get namespaces
kubectl config set-context --current --namespace=lec2
kubectl config view --minify | grep namespace: 
kubectl config view
# dry run 
kubectl run nginx-app --image=nginx --port=80 --dry-run=client -o yaml > nginx-app-pod.yaml
kubectl apply -f nginx-app-pod.yaml 
kubectl port-forward pod/nginx-app 8080:80
kubectl exec -it nginx-app -- bash

kubectl apply -f multi-container-app-pod.yaml 
kubectl port-forward pod/multi-container-app 8080:80
kubectl delete -f multi-container-app-pod.yaml

kubectl apply -f nginx-test-app-pod.yaml
kubectl port-forward pod/multi-container-app 8080:80


docker build -t ubuntu-nginx-htop:1.0 .
docker tag ubuntu-nginx-htop:1.0 munspel/ubuntu-nginx-htop:1.0
docker tag ubuntu-nginx-htop:1.0 munspel/ubuntu-nginx-htop:latest
docker push munspel/ubuntu-nginx-htop:latest
docker push munspel/ubuntu-nginx-htop:1.0

 kubectl top pod nginx-test-app
```

- https://manpages.ubuntu.com/manpages/jammy/man1/stress.1.html
