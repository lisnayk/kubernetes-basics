apiVersion: v1
kind: Service
metadata:
  name: api-proxy-service
spec:
  type: NodePort
  selector:
    app: api-proxy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
